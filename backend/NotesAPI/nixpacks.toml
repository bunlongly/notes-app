[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
  "dotnet publish NotesAPI.csproj -c Release -r linux-x64 --self-contained true -o out",
  "ls -R out"
]

[start]
cmd = "./out/NotesAPI"
