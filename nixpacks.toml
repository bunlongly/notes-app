[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
  "cd backend/NotesAPI && dotnet restore NotesAPI.csproj",
  "cd backend/NotesAPI && dotnet publish NotesAPI.csproj -c Release -o /app/out"
]

[start]
cmd = "dotnet /app/out/NotesAPI.dll"
